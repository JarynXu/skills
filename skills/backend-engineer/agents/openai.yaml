interface:
  display_name: "Backend Engineer"
  short_description: "Build, debug, review, migrate, and operate production backends"
  default_prompt: "Use $backend-engineer to choose the correct backend work mode, inspect project truth, load only relevant offline knowledge, act within authority, and verify the result according to its real risk surface."
