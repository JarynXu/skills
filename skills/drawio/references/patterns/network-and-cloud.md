# Network and cloud

Use nested containers for region, VPC/VNet, availability zone, subnet, cluster, rack, or trust boundary. Prefer official/native provider and networking stencils when exact symbols materially help recognition; preserve their raw styles.

Show traffic direction, protocol/port, trust crossing, redundancy, and active/standby semantics only when known. Do not infer security from placement alone. Keep logical and physical views separate when mixing them would be misleading.